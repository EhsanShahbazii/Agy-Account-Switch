"use strict";

function initAccountSwitcherUi() {
    if (typeof window === "undefined" || typeof document === "undefined") return;
    console.log("[AccountSwitcher] Initializing UI module...");
}

module.exports = { initAccountSwitcherUi };
